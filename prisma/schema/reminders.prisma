model Reminders {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  actions Actions? @relation(fields: [actionId], references: [actionId])

  actionId String? @unique
}
